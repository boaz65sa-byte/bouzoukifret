# יומן התקדמות — Bouzouki Academy

> קובץ זה מתעדכן בסוף כל סבב עבודה, כדי שאפשר יהיה להבין מה נעשה ומה נשאר בלי לסרוק מחדש את כל הקוד. עדכון אחרון: 2026-08-07 — "פוזיציות המודוס" עבר בדיקת-תקינות שיטתית (448 קומבינציות דרומוס/שורש/מיתרים, 2068 פוזיציות) ותוקנו 2 באגים אמיתיים. סטטוס Google Play (מ-2026-07-29) ללא שינוי: עדיין בבדיקת Closed Testing, ממתין לגיוס 12 בודקים + 14 יום.

## 2026-08-07 (המשך ב') — "פוזיציות המודוס": בדיקת-תקינות שיטתית על כל הדרומוסים/פוזיציות

המשתמש ביקש לעבור "פוזיציה פוזיציה, סולם סולם" בצורה מקצועית — לא עוד דוגמה בודדת. נכתב סקריפט בדיקה שרץ על **כל 16 הדרומוסים × 7 שורשים × 4 מספרי-מיתרים** (448 קומבינציות, 2068 פוזיציות בפועל), ובודק לכל פוזיציה: התו הראשון תואם בדיוק לדרגה המצופה, כל תו בתוך תיבת-הסריגים שהפוזיציה מבטיחה (לא "בורח" החוצה), הצליל של כל תו תואם את דרגתו, המסלול עולה מונוטונית, הפוזיציות ברצף לא-חופף. הריצה הראשונה מצאה **252 בעיות אמיתיות**.

**באג 1 — תווים "בורחים" מחוץ לתיבה שהובטחה (`note-outside-box`, `incomplete-path`)**: קרה בעיקר ב-1-2 מיתרים. `buildDegreePositionPath` השתמש ב-`placeTargetsOnNeck` — פונקציה שנועדה למסלולי-מלודיה (שם קפיצה שקטה לתיבה הבאה כשתו לא נכנס זה נכון), אבל לפוזיציה זה שקר ויזואלי: הפוזיציה מבטיחה תיבה קבועה (posBase..posBase+span), ותו שה"קפיצה" שולחת אותו רחוק ממנה שובר את ההבטחה. נוספה `placeTargetsInFixedBox` (`js/fretboard-scale.js`) — גרסה שעוצרת במקום לקפוץ; עם 1-2 מיתרים אוקטבה שלמה לא תמיד נכנסת בתיבה צרה, וזה מצב אמיתי (הפוזיציה פשוט מכסה חלק מהאוקטבה), לא באג.

**באג 2 — פוזיציות-זנב חסרות-שימוש (1-2 תווים בלבד)**: אחרי תיקון #1, פוזיציות קרוב לקצה הצוואר (או ב-1-2 מיתרים) הכילו לפעמים רק תו-שניים — טכנית תקין אבל לא שימושי לתרגול. נוסף סף-מינימום ב-`buildAllDegreePositions` (`Math.min(n+1, stringMode+1)` — מותאם למספר-המיתרים, כי תיבה צרה עם פחות מיתרים פשוט לא יכולה להכיל אותו מספר תווים כמו עם יותר מיתרים; סף אחיד היה מסנן כמעט הכל ב-1 מיתר, כי המקסימום האמיתי המושג שם הוא 4 תווים, לא 8). פוזיציות מתחת לסף מדולגות (לא נכללות ברשימה), אבל החיפוש ממשיך לדרגה הבאה — לא עוצר.

**התוצאה אחרי שני התיקונים**: 0 בעיות מתוך 2068 פוזיציות. אומת גם רגרסיה: `buildScaleDegreePath` (הפונקציה המקורית, בשימוש 9+ מסכים) עדיין מחזירה תוצאה זהה ב-100%.

**היסטוריה מלאה של הסבב הזה** (לתיעוד, לא לחזור עליו): נוסה גם עיגון-פוזיציה קבוע על מיתר D (כדי ש"פוזיציה 1" תמיד תתחיל במיתר פתוח) — זה גרם לכל המסלול לקרוס למיתר D בלבד (12 סריגים על מיתר אחד, לא תיבה צרה מרובת-מיתרים) כי D הוא המיתר הכי-דק בכיוונון ואין "מעלה" ממנו לעבור אליו; המשתמש תפס את זה ויזואלית (מספרים על מיתר אחד, שמות-תווים מנותקים על השאר) ותוקן חזרה לעיגון על המיתר הכי-נמוך הפעיל (`js/fretboard-scale.js:findDegreeAnchor`, ראו הערה בקוד).

**תקלת cache חוזרת**: ה-Service Worker נרשם מחדש בכל טעינת דף (זה הקוד התקין של האפליקציה עצמה) — כל בדיקה אחרי עריכת קובץ דרשה ניקוי-cache מפורש (`serviceWorker.getRegistrations()...unregister()` + `caches.delete`) לפני ריענון, אחרת נבדק קוד ישן בשקט.

## 2026-08-07 (המשך) — מסך חדש "פוזיציות המודוס" (7 פוזיציות לפי דרגה, לכל דרומוס)

בעקבות הדיון על "קול שני לדואט", המשתמש ביקש מנגנון כללי יותר: לכל דרומוס/מודוס/סקייל (7 תווים) יש 7 "פוזיציות" קלאסיות על הגריף — פוזיציה N מתחילה בדיוק כשדרגה N היא התו הנמוך ביותר, ורצות ברצף עולה על פני כל הצוואר (בדיוק כמו שיטת הפוזיציות בגיטרה).

**גילוי לפני שנכתב קוד**: המערכת הקיימת (`FretboardScale.buildScaleDegreePath` + צ'יפי-פוזיציה "0/2/3/5/7/9/12") **כבר** נותנת בכל תיבת-פוזיציה את כל 7-8 תווי הסולם (עם `findBestPositionForStringMode` שמזיז אוטומטית את התיבה כשמחליפים מספר-מיתרים) — זה אומת בפועל בדפדפן עם "סבאח" לפני שנכתב שורת קוד אחת, ונמצא שזה כבר עובד גנרית לכל דרומוס. אבל זו לא "פוזיציה N = דרגה N" — זה סתם סריגים קבועים שרירותיים.

**הבנייה בפועל**:
- `js/fretboard-scale.js`: `buildScaleDegreePath` פוצל — לוגיקת ההנחה-על-הגריף (walk targets, בחירת סריג/מיתר, הרחבת תיבה) הוצאה לפונקציה משותפת `placeTargetsOnNeck`, בלי לשנות התנהגות (אומת רגרסיה: תוצאה זהה ב-100% ל-4 דרומוסים לפני/אחרי).
- פונקציות חדשות: `findDegreeAnchor` (מוצא את הסריג הכי-נמוך על המיתר הכי-נמוך הפעיל שבו יושבת דרגה נתונה), `buildDegreePositionPath` (בונה מסלול סולם-מלא שמתחיל בדיוק על דרגה נתונה, לא תמיד השורש), `buildAllDegreePositions` (רץ ברצף על כל הצוואר — כל פוזיציה מתחילה איפה שהקודמת הפסיקה, עד גמר 15 הסריגים). כולן גנריות — עובדות על `intervals` בלבד, לא הרדקוד לפי שם-דרומוס.
- **אומת בדפדפן על 5 דרומוסים** (חיג'אז, סבאח, ראסט, אוסאק, חוזאם): לכל אחד, `firstNote` של כל פוזיציה תואם בדיוק לדרגה המצופה (D→E→F→F#→A→A#→C לסבאח, למשל), ל-4 ול-1 מיתרים כאחד.
- מסך חדש `js/mode-positions.js` ("פוזיציות המודוס", נגן `data-screen="mode-positions"`) — בורר דרומוס/שורש/מספר-מיתרים, צ'יפי פוזיציה (6-7 בד"כ, תלוי כמה נכנס עד סריג 15), לוח גריף + רשימת-תווים ממוספרת, וניגון עולה-יורד (`FretboardScale.scalePlaySequence`). **אומת ניגון**: 15 קריאות `pluckCourse` נכונות (8 עולה + 7 יורד, פלינדרום מדויק).

**נשאר בכוונה בחוץ**: לא נגעתי בשום מסך קיים (9+ מסכים עם `mountDromosScalePanel` ו-`DromosOverrides` השמורים לפי סריגים ישנים) — זה מסך עצמאי חדש, לא שינוי-התנהגות למסכים קיימים. אם ירצה בעתיד לשלב את מערכת-הדרגות גם בצ'יפים הקיימים, זה ידרוש טיפול זהיר בסיכון-orphan ל-overrides שמורים.

## 2026-08-07 — "קול שני לדואט": תוקן באג-עיצוב אמיתי שנתפס ע"י המשתמש

המשתמש דיווח שהצליל והמיקומים של קול 1 וקול 2 "כמעט זהים" — לא ניחוש, אלא תצפית אמיתית. בדיקה בדפדפן אישרה: הגרסה הקודמת של `js/duet-voices.js` חישבה קול שני מתוך **ריצת-סולם מלאה** (`FretboardScale.buildScaleDegreePath` — כל הסולם בתיבת-פוזיציה, לא מנגינה אמיתית). הזזת ריצת-סולם שלמה ב-N דרגות בתוך עצמה מחזירה מתמטית **בדיוק את אותן 7 תווי-הסולם** (מחזור סגור), ובנוסף אלגוריתם בחירת הסריג העדיף את אותו מיתר שהתו המקורי היה עליו — כך שבפועל שני הלוחות הציגו כמעט אותן נקודות פיזיות. אומת בפועל: מיתר A סריג 1 הופיע עם אותה תווית-דרגה בדיוק בשני הלוחות.

**הפתרון (בהתייעצות עם המשתמש, לא ניחוש)**: הכלי נבנה מחדש לעבוד על **מנגינה קצרה אמיתית** שהמשתמש מזין בעצמו, לא ריצת-סולם:
- קלט: אותו פורמט טקסט-טאב שנבנה קודם ל-SongTeacher (`D:`/`A:`/`F:`/`C:` + `BPM:`/`STEP:`) — `SongTeacher.parseTextTab` יוצא עכשיו גם כ-API ציבורי כדי ש-`DuetVoices` ישתמש בו ישירות (`js/song-teacher.js`, שורת ה-`return` בסוף הקובץ).
- לכל תו במנגינה: מוצאים את הדרגה שלו בדרומוס שנבחר (`intervals.findIndex`), מזיזים ב-N דרגות (ברירת מחדל טרצה מתחת), ומחפשים סריג/מיתר קרוב לתו המקורי — עם **העדפה מכוונת למיתר שונה** מהמקור (לא רק "הכי קרוב"), כדי למנוע התנגשות פיזית.
- **אומת בדפדפן עם מנגינה קצרה אמיתית** (D D F# G A G F# D על מיתר D בלבד, חיג'אז/D): קול שני יצא **כולו על מיתר A** (D#,D,A#,F#... — סט תווים שונה בחלקו מקול 1), לא אותו סולם. ניגון-ביחד אומת: 16 קריאות `pluckCourse` נכונות ב-8 צעדים מסונכרנים, זוגות (D,A) בכל צעד תואמים בדיוק לחישוב.
- **תקלת בדיקה נפרדת שנתקלנו בה**: אחרי כתיבת הקוד החדש, טעינה חוזרת של אותו טאב בדפדפן המשיכה להריץ את הגרסה הישנה בגלל HTTP cache על `js/duet-voices.js` (`transferSize:0` גם אחרי ניווט-מחדש/Ctrl+Shift+R) — נפתר ע"י פתיחת טאב דפדפן חדש לגמרי. שווה לזכור לסבבים הבאים: reload רגיל לא תמיד מספיק כדי לראות שינוי בקובץ JS בסביבת התצוגה המקדימה הזו.

## 2026-08-06 — כלי "קול שני לדואט" + קלט טאב-טקסט/עורך ידני ל-SongTeacher

**"קול שני לדואט" (`js/duet-voices.js`, מסך חדש)**: בוחרים דרומוס/שורש/פוזיציה לקול ראשון (דרך `FretboardScale.mountDromosScalePanel` הקיים) — קול שני מחושב אוטומטית כהרמוניה דיאטונית (מרחק-דרגות קבוע, ברירת מחדל טרצה מתחת, ניתן לשינוי ל-2–6 דרגות ומעל/מתחת) ומוצג בלוח נפרד באותה תיבת-פוזיציה, כדי ששני נגני בוזוקי ינגנו יחד. כולל ניגון: קול 1 לבד / קול 2 לבד / שניהם ביחד בסנכרון (`AudioEngine.pluckCourse` + `FretboardScale.flashMidi`, אותו מנגנון קיים בשאר האפליקציה). **אומת בדפדפן**: חושבתי עצמאית (קוד מקביל) את ההרמוניה לחיג'אז על D בשני הכיוונים והשוויתי תו-מול-תו לתוויות שצוירו בפועל — התאמה מלאה; ניגון-ביחד אומת ע"י ספירת קריאות `pluckCourse` בפועל (15/16 תקין, דילוג חלק על התו היחיד שלא נכנס לתיבה).

**קלט טאב-טקסט + עורך ידני ב-SongTeacher (`js/song-teacher.js`, "למד אותי את השיר")**: המסך כבר תמך בהעלאת אודיו (AI transcription) או JSON מוכן; נוסף עכשיו קלט שלישי בלי אודיו בכלל — (א) תיבת טקסט לפורמט טאב פשוט (`D:`/`A:`/`F:`/`C:` שורה למיתר, `CH:` אקורדים אופציונלי, `BPM:`/`STEP:`), (ב) עורך ידני תו-אחר-תו (בחירת מיתר+סריג+משך, בלחיצות). שניהם ממירים ל-`{tabNotes,chords,bpm}` ומזינים ישירות ל-`loadAnalysis()` הקיים — כל מנוע הניגון/הדגשת-הגריף/האטה/לולאה נשאר ללא שינוי. **באג אמיתי נתפס ותוקן תוך כדי בדיקה בדפדפן**: רגקס הפרסור בדק את התווית `C` לפני `CH` (סדר האלטרנציה ברגקס), כך ששורת `CH:` (אקורדים) הייתה מתפרשת בשקט כזבל על שורת מיתר C והאקורדים נעלמו (0 אקורדים בפלט) — לא נתפס בקריאת קוד, רק כי הרצתי את הפרסר בפועל וספרתי אקורדים. תוקן ע"י סידור-מחדש `(CH|D|A|F|C)`. **הערת זכויות יוצרים חשובה שהועברה למשתמש**: לא בוצע (ולא יבוצע) חיפוש-רשת/העתקה של טאבים/תווים מתומללים של שירים ספציפיים לתוך האפליקציה — זהו סיכון זכויות-יוצרים ידוע (ראה סעיף 2026-07-16 למטה), הכלי מיועד לכך שהמשתמש עצמו יזין תוכן שבבעלותו/ברשותו. הוחלט במפורש לא לבנות קלט PDF/תמונה (OMR) בסבב הזה — תחום לא-בשל בדפדפן, לא מדויק, ולא מתאים לארכיטקטורת vanilla-JS בלי build-step של הפרויקט; לשקול מחדש רק אם יתברר שצריך.

## 2026-07-29 (המשך) — הגשה בפועל ל-Google Play (Closed Testing) + תיקון "בתשלום"→"חינם"

אחרי עיצוב האייקון החדש (ראו סעיף למטה) ו-rebuild של ה-AAB (versionCode 3), המשתמש ניסה לעבור ל-Production אבל התגלה שחשבונות מפתח חדשים ב-Google Play חייבים מסלול "בדיקה בקבוצה מוגדרת" עם 12 בודקים במשך 14 יום לפני שגישת Production בכלל נפתחת — מדיניות רשמית של גוגל, נבדק ישירות ב"מרכז השליטה". במקום לעצור שם, הוקם בפועל מסלול הבדיקה הסגורה:

- הגרסה **קודמה** (promote, לא הועלתה מחדש) ממסלול הבדיקה הפנימית למסלול "Alpha" — Play Console מעתיק אוטומטית את אותו AAB.
- נתקלנו בטיוטה תקועה (משגיאת "קוד גרסה כבר בשימוש" מניסיון קידום כפול) — נמחקה ידנית.
- לא נבחרו מדינות הפצה — נבחרו כל המדינות.
- **הבאג האמיתי שחסם את כל התהליך**: האפליקציה הייתה מוגדרת ב-Play Console כ"בתשלום" בלי merchant account מחובר — לא ניתן היה לשמור אף גרסה בשום מסלול בגלל זה, לא רק Production. תוקן ל"חינם" תחת `תמחור האפליקציה` (התאמה למה שהאפליקציה תמיד הייתה אמורה להיות — אין רכישות/IAP).
- הגרסה נשמרה בהצלחה, ו-13 השינויים המצטברים (הצהרות תוכן + דף חנות + תמחור + גרסת הבדיקה) **נשלחו בפועל לבדיקת Google**.

**נשאר**: לגייס 12 בודקים אמיתיים (Gmail) ולהמתין 14 יום רצופים לפני שאפשר להגיש בקשת גישה ל-Production. ראו `STORE-SUBMISSION.md` לפירוט מלא.

---

## 2026-07-29 — עיצוב אייקון חדש + rebuild מלא של ה-AAB/APK

**אייקון**: הוחלף העיגול/מוט המופשט (`icons/icon.svg` הישן) באיור בוזוקי מפורט ומקצועי — גוף אגס-צורה עם גרדיאנט זהב, חור-קול עם רוזטה דקורטיבית, גשר, צוואר עם 6 סריגים ושתי נקודות-סימון, וראש עם 8 מפתחות כיוונון אמיתיים (4+4, תואם למבנה הכלי בפועל). נבנה כ-SVG יחיד ונרנדר לכל הגדלים הדרושים (`icons/icon-{192,512}.png`, `icons/icon-512-maskable.png`, `store-assets/app-icon-1024.png`) דרך Chrome headless מקומי — נבדק פיקסל-לפיקסל שאין שקיפות (כדי לא לחזור על באג ה-75%-שקיפות מסבב 2026-07-16), ונבדק ויזואלית שקריא גם בגודל 128px.

**Rebuild**: המשתמש הריץ מחדש `npx @bubblewrap/cli build` (עם versionName נשאר `1.0.0`, **versionCode עלה מ-2 ל-3** — חובה כי גוגל דורשת קוד גרסה עולה בכל העלאה). נתקל בתקלת "אין מספיק זיכרון להפעיל את Gradle daemon" (רק 525MB פנויים מתוך 7.8GB) — נפתר ע"י הורדת `org.gradle.jvmargs` ב-`mobile/android/gradle.properties` מ-`-Xmx1536m` ל-`-Xmx1024m` וסגירת תהליכים. ה-build הצליח: `app-release-bundle.aab`/`app-release-signed.apk` עודכנו (14:56, גדלו מ-1.2MB ל-2.1MB — תואם לאייקון החדש שנארז בפועל בכל צפיפויות ה-mipmap).

**נשאר**: העלאת `mobile/android/app-release-bundle.aab` (versionCode 3) ל-Play Console, ועדיין ההעלאה הידנית של 3 קבצי הגרפיקה לדף החנות (ראו סעיף קודם).

---

## 2026-07-28 — Google Play: כל ההצהרות + דף החנות הראשי הושלמו

בין הסבב הקודם (2026-07-16, שם נכתב ש-build ה-Android לא ניתן להשלמה בסביבה ההיא) לבין הסבב הזה, המשתמש הריץ בפועל את `bubblewrap build` על המכשיר שלו — נוצרו keystore חתימה אמיתי, `app-release-bundle.aab` ו-`app-release-signed.apk` (קיימים ב-`mobile/android/`, מוגנים ב-`.gitignore`), ומולא ה-fingerprint האמיתי ב-`.well-known/assetlinks.json` (קומיטים `8cd4f09`, `906c417`). **אומת בסבב הזה**: assetlinks.json חי בפרודקשן זהה ב-100% לקובץ המקומי — אימות Digital Asset Links תקין.

**עבודה שנעשתה בסבב הזה** (דרך אוטומציית דפדפן ב-Play Console, כל שמירה אושרה מראש מול המשתמש):
- כל 10 ההצהרות הנדרשות בעמוד "תוכן האפליקציה" הושלמו: מדיניות פרטיות, מודעות (ללא), פרטי כניסה (ללא הגבלות), סיווג תוכן IARC (קטגוריה "כל סוגי האפליקציות האחרים", "שימוש עיקרי=חינוך" → תוצאה **PEGI 3 / USK 0 / IARC Generic 3+**, Everyone בכל האזורים), קהל יעד (כל הגילאים — כולל אישור נפרד ל-COPPA/GDPR-K מאחר ואין SDK/API של צד שלישי ואין איסוף נתונים; לא הצטרפות ל-Teacher Approved), אבטחת נתונים ("לא נאסף/לא משותף" + תג התחייבות ל-Families Policy), אפליקציות ממשלתיות/תכונות פיננסיות/בריאות (כולן "לא רלוונטי"), מזהה פרסום Android 13 (לא בשימוש).
- דף החנות הראשי: קטגוריה נבחרה (חינוך), פרטי קשר מולאו (`boaz65sa@gmail.com` + `bouzoukifret.vercel.app`), שם/תיאור קצר/תיאור מלא מולאו מהטקסט המוכן מראש ב-`STORE-LISTING.md`.
- אומת ויזואלית: `store-assets/play-store-feature-graphic.png` — הטקסט העברי קריא ותקין (לא הפוך), אין דלוקה.
- אומת בדפדפן: האתר הרגיל (לא TWA) עדיין מציג את מסך "למד מהשיר (YouTube)" כרגיל — StoreMode gating לא משפיע על גרסת האתר.

**נשאר למשתמש בלבד** (חוסם אוטומציה, לא חוסר-ידע): להעלות ידנית 3 קבצי גרפיקה ל-Play Console (אייקון, feature graphic, screenshots — כלי הדפדפן לא יכול לגשת לקבצים מקומיים מחוץ לתיקיית השיתוף שלו) ולצלם 6 מסכים אמיתיים של האפליקציה (עדיין לא הופקו). אחרי זה: מעבר ממסלול Internal Testing (שכבר יש בו release מאושר עם ה-AAB) למסלול Production ושליחה לבדיקה.

---

## 2026-07-16 (המשך) — הכנה לפרסום בחנויות

בוצע audit מקיף (4 סוכני מחקר: תוכן/זכויות-יוצרים, פרטיות/הרשאות, סיכוני-מדיניות, מבנה-PWA) לפני התחלת עבודת הקוד. **ממצא קריטי שהתגלה**: `js/songs/song-catalog.js` הכיל מילות שיר אמיתיות (בית+פזמון) ל-132 שירים מוגנים עם אמנים מזוהים בשם — סיכון משפטי אמיתי, לא רק סיכון דחייה מחנות. המשתמש החליט להסיר בגרסת החנות בלבד (האתר נשאר כמו שהוא).

**המנגנון המרכזי**: `js/store-mode.js` — מודול IIFE חדש שמזהה runtime אם האפליקציה רצה בתוך TWA (Android, `document.referrer` מתחיל ב-`android-app://`) או Capacitor (iOS, `window.Capacitor.isNativePlatform()`). **אותם קבצים בדיוק** בין אתר לחנות — אין build נפרד, שומר על העיקרון של "בלי שלב build" של הפרויקט. על בסיס הדגל הזה: מסך "למד מהשיר (YouTube)" (ותלוייו: ספריית לימוד, Φωνή→Bridge) מוסתר לגמרי מהניווט, ומילות השירים מתאפסות פעם אחת בזיכרון אחרי טעינת `SONG_CATALOG` (אקורדים/שם/אמן/מבנה נשארים) — שני מנגנוני ה-gating נבדקו ואומתו בדפדפן (גם ה-flag דלוק וגם כבוי).

**באג אמיתי נתפס ותוקן** (לא קשור לחנויות, שווה לתקן בכל מקרה): `index.html` קרא ל-`BouzoukiTuner.init()`/`SongLibrary.init()` בלי guard מסוג `typeof X !== 'undefined'` שכל שאר הבלוק משתמש בו — פתיחה ראשונה-אי-פעם אופליין (לפני קאש) הייתה יכולה לזרוק חריגה שעוצרת גם את רישום ה-Service Worker. תוקן ע"י הזזת רישום ה-SW להיות ראשון וללא-תלות, ועטיפת שאר האתחולים ב-try/catch.

**נכסים חדשים**: `privacy.html` (מדיניות פרטיות מלאה, superset של גרסת האתר), אייקון maskable (`icons/icon-maskable.svg` + `icons/icon-512-maskable.png` — **הראשון שנוצר דרך רינדור-דפדפן התברר שבור: 75% מהקנבס יצא שקוף בטעות בגלל flakiness בטכניקת ה-canvas/screenshot; זוהה רק ע"י בדיקת פיקסלים ישירה, לא ע"י תצוגה חזותית — נבנה מחדש בפייתון (Pillow) עם חישוב גיאומטרי ישיר, מאומת: 0% שקיפות, תוכן בתוך אזור 80% הבטוח**), `.well-known/assetlinks.json` (תבנית עם placeholders ל-Digital Asset Links), `manifest.json` עודכן (id, categories, ערכי maskable).

**מסלול אריזה**: TWA לאנדרואיד (עוטף את האתר החי ישירות) + Capacitor ל-iOS (עוטף עותק מקומי, `mobile/capacitor.config.json` + `scripts/prepare-capacitor-www.js`/`npm run mobile:www` — מאומת: מעתיק 75 קבצים נכונים, בלי `api/`/`tools/`/סודות). **לא ניתן היה להשלים כאן**: `npx @bubblewrap/cli init` אינטראקטיבי ומבקש להתקין JDK (נבדק, נכשל כצפוי בסביבה הזו בלי טרמינל אינטראקטיבי); בניית ה-APK/AAB בפועל דורשת Android Studio+JDK; iOS דורש Mac+Xcode לגמרי (לא זמין כאן בשום צורה). כל הפרמטרים/פקודות המדויקים מתועדים ב-`STORE-SUBMISSION.md` להמשך על המכשירים של המשתמש, כולל טפסי Data Safety/App Privacy מוכנים-להעתקה (מתארים את **הגרסה המצומצמת** בפועל של גרסת החנות — לא את הסופרסט של האתר).

## מה נעשה (מהחדש לישן)

### 0. חמשת הסעיפים הפתוחים מ-PROGRESS.md — כולם טופלו
1. **YouTube — דיאלוג "הורדה למחשב" מותאם מובייל**: `js/learn-youtube.js` — שני נקודות-קריאה נוספות ל-`_showProxySetupModal()` (הבאנר + אקורדיון ההגדרות) עכשיו בדוקות `DeviceUtils.isMobile()`, במובייל מוצג טקסט קצר במקום הוראות Windows/yt-dlp.
2. **מירור אנכי לדיאגרמות אקורד**: `js/chord-diagram.js` — `draw()`/`miniHTML()` קוראים עכשיו גם ל-`FretboardMirror.isV()`, עם `fretY()`/`dotY()` שהופכות את הגריד סביב מרכז הצוואר (נוט זז לתחתית, לא CSS transform — אותו עיקרון כמו `courseY` הראשי).
3. **Root-invariant override — checkbox אופציונלי**: `js/dromos-overrides.js` (`setInvariant`/`getInvariant`/`getKind`/`resetInvariant`, מפתח `dromosId|*|posBase|stringMode`) + `js/fretboard-scale.js` (`FretboardScale.saveInvariantOverride`/`resolveOverridePath`/`resolveInvariantPath`). ברירת המחדל (מדויק-לטוניקה) נשארת; checkbox "החל על כל הטוניקות" ב-`mountDromosScalePanel` וב-`initDromoi()` שומר יחסית ל-**היסט-מיתר מהעוגן** (`courseOffset = p.ci - anchor.ci`) ולא מיתר אבסולוטי — **תוקן תוך כדי בדיקה**: גרסה ראשונה ששמרה מיתר אבסולוטי נשברה בשקט (כל הנקודות נופלות מחוץ לתיבה) בכל טוניקה שבה השורש יושב על מיתר אחר; הפתרון: לשמור היסט יחסי לעוגן, לא מיתר מוחלט.
4. **עריכת אקורדים — לחיצה ישירה על הדיאגרמה**: `js/chord-diagram.js` `draw()` מקבל `editable`+`onFretEdit` (תא-סריג לחיץ קובע ישירות, לא מחזור-בלחיצה; "×"/עיגול-פתוח תמיד לחיצים במצב עריכה). `js/chord-library.js` — `ChordLibrary.setFret()` חדש, עוטף `upsertFromForm` ושומר על `he`/`cat`/`desc` קיימים. הופעל דרך `renderCard({editable:true})` ב-`js/exercises.js` בלבד (יעד ראשי); שאר 9 הצרכנים של `ChordDiagram`/`ChordTooltip` נשארו קריא-בלבד לעת עתה — אפשר להדליק בלי שינוי בליבה.
5. **theory-lab / dromos-road — כפתור "✒️ ערוך" (deep-link)**: לא נבנה מנוע עריכה שלישי (ההחלטה הקודמת "להשאיר כך" השתנתה לכיוון deep-link, לא בוטלה). כפתור חדש ליד בורר הפוזיציה בשני המסכים קורא ל-`openDromoiForEdit({dromosId, rootPc, posBase, stringMode})` חדש ב-`js/app.js` — מזין את מסך `dromoi` הקיים באותו מצב ופותח אותו ישירות במצב עריכה (מדמה קליק על `#dromoi-edit-toggle`, לא בונה state חדש). אגב: נוספו `data-course`/`data-fret` ל-`tl-neck-dot` ב-theory-lab.js (הכנה זולה לאיחוד עתידי).

אומת בדפדפן (לא רק syntax check): זרימת עריכת אקורד מקצה-לקצה כולל `syncToGlobalChords()`; מירור אנכי הופך קואורדינטות נכון; **טרנספוזיציית root-invariant אומתה בפועל** על "חיג'אז" בין טוניקת D ל-G — הבאג באחסון-מיתר-אבסולוטי נתפס ותוקן רק בזכות הבדיקה בדפדפן, לא ב-code review; שני כפתורי ה-deep-link מנווטים נכון למסך dromoi במצב עריכה.

### -1. דפי עבודה — עיצוב אקורדים מאוחד עם השיעורים
- דפי עבודה מסוג "דרומוסים" כבר השתמשו במנוע המשותף (`mountDromosScalePanel`), אבל דפי עבודה מסוג "אקורדים" השתמשו בציור SVG נפרד ופשוט (`chordSvg`) שלא תאם ויזואלית ל-`ChordDiagram` שמשמש בשאר האפליקציה.
- `js/chord-diagram.js`: `draw()` קיבל פרמטר `theme` ('dark'/'print') — כל הצבעים הקשיחים הוחלפו בטבלת צבעים לפי ערכת נושא, כדי שיהיה אפשר לצייר גם על רקע כהה (באפליקציה) וגם בניגודיות גבוהה על נייר לבן (הדפסה) מאותה פונקציה. נוסף גם `rootPc` — מדגיש את תו-השורש של האקורד בצבע נפרד (פיצ'ר פדגוגי שהיה קיים רק ב-worksheets ולא ב-ChordDiagram; עכשיו קיים בשניהם).
- `js/worksheets.js`: `chordSvg()` נבנה מחדש כעטיפה דקה סביב `ChordDiagram.draw(svg, {theme:'print', rootPc})` (יוצר SVG זמני מנותק, קורא לפונקציה המשותפת, מחזיר `outerHTML`) — באותו דפוס בדיוק כמו `buildPrintFretboardCapture` הקיים לדרומוסים.
- אומת: מסך "כרטיסי אקורדים" הרגיל באפליקציה (ערכת dark) לא השתנה; דפי עבודה (ערכת print) מציגים עכשיו הדגשת-שורש נכונה (נבדק חישוב pitch-class מדויק על אקורד לא-סימטרי).

### 0. דפי עבודה — פרטבורד עם מסלול דרומוס להדפסה/שמירה — `b14457e`
- במסך **דפי עבודה** לכל דרומוס שנבחר מוצג פרטבורד אינטראקטיבי עם מסלול הדרומוס (דרך `FretboardScale.mountDromosScalePanel`): בחירת פוזיציה ומספר מיתרים, תווית על הנקודה = דרגה·אצבע.
- **לחיצה על תו** מציגה כרטיס מידע: שם בעברית, בלועזית, מיתר, סריג ומספר האצבע; מתחת ללוח — רשימת מסלול מלאה להדפסה.
- **עריכת דף העבודה** — כפתור לפתיחה/סגירה של בחירת אקורדים/דרומוסים והערות; על הלוח נשארת גם «ערוך פוזיציה» (דרך `DromosOverrides`) כמו בשאר המסכים.
- **הדפסה / שמירת HTML** לוכדות את ה־SVG החי מהתצוגה המקדימה (כולל מטא־פוזיציה ורשימת המסלול) ומוציאות אותו ל־iframe הדפסה / קובץ עצמאי.
- `fretboard-scale.js`: `onDotClick` הועבר דרך `renderDromosScale` / `mountDromosScalePanel` / מצב עריכה, כדי שמסכים יוכלו להציג מידע על התו בלי לגעת במנוע הציור.

### 1. בחירת מיתרים = בחירת "דרך" אוטומטית (לא רק סינון)
- באג שהמשתמש מצא: בחירת "4 מיתרים" בפוזיציה מסוימת (למשל חיג'אז בפוזיציה 9) הציגה מסלול שבפועל עבר רק ב-3 מיתרים — כי במיקום הזה מיתר הדו פיזית לא מגיע לתווים הנדרשים. זו התנהגות מוזיקלית נכונה, אבל המשתמש רצה שבחירת מספר-מיתרים תהיה "דרך" אמיתית — שהמערכת תמצא בעצמה פוזיציה שבה יש בפועל מסלול ב-N מיתרים.
- פונקציה חדשה `FretboardScale.findBestPositionForStringMode(intervals, rootPc, stringMode, bases, span)` — בודקת את כל בסיסי-הפוזיציה המועמדים, סופרת כמה מיתרים שונים המסלול בפועל מנצל בכל אחד, ומחזירה את הטוב ביותר (עם הכרעת תיקו לפוזיציה הנמוכה).
- חוברה לכל 3 מקומות בחירת המיתרים: `mountDromosScalePanel` (מכסה ~9 מסכים), `mountPosControls`/`master.js` (כולל תיקון בעיית "closure מיושן" — הבורר לא התעדכן כשהוחלף דרומוס), ו-`#dromoi-pos-bar` במסך dromoi. קליק ידני על צ'יפ פוזיציה עדיין עובד רגיל (לא נדרס).
- אומת: לכל 4 מספרי המיתרים (1-4) על חיג'אז, הפוזיציה שנבחרת אוטומטית נותנת בדיוק את מספר המיתרים המבוקש בפועל.

### 2. עורך פוזיציות/אצבוע מותאם אישית לדרומוי — `fd871df`
- מודול חדש `js/dromos-overrides.js` — dictionary ב-localStorage (`bouzouki-position-overrides-v1`), מפתח לפי `דרומוס+שורש+פוזיציה+מיתרים`.
- הזרקה יחידה בתוך `buildScaleDegreePath` (fretboard-scale.js) — אם יש override הוא מוחזר ישירות, כך שכל מסך/נגינה שמזין דרך הפונקציה הזו (ישירות או דרך `renderDromosScale`/`mountDromosScalePanel`) מקבל אותו אוטומטית.
- עריכה בקליק ישיר על הפרטבורד: מחזור מצבים — לא-כלולה→אצבע1→2→3→4→מוסרת.
- ממשק שמור/ביטול/איפוס + באדג' "✓ מותאם", מובנה ב-`mountDromosScalePanel` (מכסה ~7 מסכים: modus-path, song-academy, features, features2, dromos-visuals) **וגם** במסך `dromoi` (הבורר העצמאי שלו).
- אומת קרוס-מסך בפועל: override שנשמר במסך אחד מופיע זהה במסך אחר + בהשמעת אודיו.
- תוקן תוך כדי: `ChordDiagram.draw()` לא ניקה SVG לפני ציור מחדש (הכפלת אלמנטים בכל טוגל מירור).
- **תיקון נוסף אחרי בדיקת המשתמש**: אחרי מירור אופקי, פרטבורד ברוחב-גלילה (theory-lab) לא עדכן את מיקום הגלילה — התוכן הרלוונטי (מיתר פתוח) עבר לצד השני אבל התצוגה נשארה במקום. שורש הבעיה כפול: (א) מיכל הגלילה של theory-lab לא הצהיר `dir="ltr"` בניגוד למנוע הראשי, כך שירש RTL מהדף וקיבל מוסכמת scrollLeft שונה; (ב) תיקון הגלילה עגן על אלמנט שמתאפס ברינדור מחדש. תוקן ב-`js/fretboard-mirror.js` + `js/theory-lab.js`.

### 3. מירור פרטבורד + בקרת מהירות + וידוא פוזיציות — `babd9b4`
- `js/fretboard-mirror.js` — כפתור ⇋/⇅ (הפוך אופקי/אנכי) בכל תצוגת פרטבורד, כדי שהתצוגה תשקף מבט על מישהו שמנגן מולך. מיושם ע"י שינוי פונקציות הקואורדינטות (`fretX`/`courseY`) ולא CSS transform — כך שהטקסט נשאר קריא.
- `js/playback-speed.js` — מכפיל מהירות ×0.5–×1.5 לכל אנימציית מסלול/נגינה (DromosRoad, theory-lab, song-academy, dromos-visuals, audio.js).
- שלושה "יוצאים מהכלל" שלא עוברים דרך המנוע הראשי (`theory-lab.js`, `reference-cards.js`, `chord-diagram.js`) קיבלו טיפול נפרד למירור.
- פוזיציות מלאות (בורר פוזיציה+מיתרים) — התברר שכבר עבד בכל מסכי הדרומוי הרלוונטיים; לא נדרש קוד חדש.

### 4. תיקוני CI, PWA ותשתית — `5fe4831`, `34f1719`, `fd94582`
- CI workflow (smoke-test) חזר לפעולה אחרי הוספת scope `workflow` לטוקן GitHub.
- באנר התקנת PWA ל-iOS/Safari (שלא תומך ב-`beforeinstallprompt`).
- Dockerfile של stem-proxy עבר ל-`npm ci` עם lockfile (build רפרודוסבילי).
- הוסר `tools/stem-proxy/server.mjs` (קוד מת).
- נוסף `npm test` שמריץ את `tools/smoke-test.js`.
- תוקנו: אצבועי אקורדים שגויים ב-`data.js`, כפילות סולמות בין `song-academy.js` ל-`data.js` (אוחד למקור אמת יחיד), באג אוקטבה/קוינטה ב-`_pos1FromDromos`, cache headers ב-`vercel.json` (immutable→must-revalidate, כי אין hash בשמות קבצים).

---

## 2026-07-16 (המשך) — סגירת 3 הסעיפים הפתוחים

### עריכת אקורדים — הורחבה לכל המסכים הרלוונטיים, כולל תיקון באג אמיתי
- **באג אמיתי שנתפס ותוקן**: ב-`songs.js`, `_drawSongStrumStrip()` (רצועת הליווי בפרטי שיר) קרא ל-`ChordTooltip.bindContainer(container)` **בלי** `editable`, לפני ש-`_bindSongChordTooltips()` קרא לאותו container **עם** `editable:true` — בגלל guard ה-`dataset.chordBound` בתוך `bindContainer`, הקריאה הראשונה "זכתה" וה-editable השני היה no-op שקט. רצועת הליווי בפרטי שיר מעולם לא הייתה ניתנת לעריכה בפועל, למרות שנראה שכן. תוקן.
- **תשתית**: `ChordTooltip.renderInto()` קיבל תמיכת `editable` (חסרה עד עכשיו) דרך פונקציה משותפת `_buildEditableChordDiagram()` (גם `show()` הפכה להשתמש בה — פחות שכפול קוד).
- **הודלק** גם ב: `exercises.js` (רצועת strum), `song-academy.js` (תחנת אקורדים), `modus-path.js` (תחנת אקורדים — גם `renderInto` וגם `bindContainer`).
- **נשאר קריא-בלבד בכוונה** (הוערך ונמצא לא-מתאים): `features.js` (ליווי ג'אם חי מתקדם אוטומטית), `song-teacher.js` (תיבת אקורד מסונכרנת עם נגינה חיה), `practice-library.js`/`song-analyzer.js`/`song-learn.js` (עיון/זיהוי, לא תרגול), `reference-cards.js` (מכוון לשקף כרטיס מודפס בדיוק), `worksheets.js` (theme='print').

### מירור לכרטיסי אקורד ב-theory-lab.js (מנוע רביעי נפרד) — הושלם
- `buildChordDiagram()`/`buildChordCard()` קיבלו מירור אופקי+אנכי מלא (לא היה שם בכלל, לא רק אנכי). **באג נתפס ותוקן תוך כדי**: כפתור המירור מוחק את עצמו בכל טוגל אם הוא מותקן בתוך האלמנט שה-redraw מנקה (`innerHTML=''`) — נפתר ע"י התקנת הכפתור באלמנט-אח נפרד. עריכה נשארת לא בסקופ (דאטהסט `chordCurriculum` שונה, כולל ברים/'B'/'B-1' לא-מספריים שלא תואמים את סכימת `ChordLibrary`).

### כפילות קוד — טופלה חלקית, השאר סגור בהחלטה מתועדת (לא ניקוי-קוד עיוור)
- **`FretboardScale.mountPosControls`** קיבל תמיכת `positionsFn` (רשימת פוזיציות דינמית) — עכשיו יכול לשמש גם תרחישים כמו dromos-road's `availablePositionBases`, לא רק "הכי טוב לפי ניחוש".
- **`js/app.js`'s `#dromoi-pos-bar` — נבדק ובוטל במכוון**: ניסיתי למזג אותו ל-`mountPosControls` (אותה מרקאפ בדיוק), אבל זה שבר את מסך dromoi בשקט — `initDromoi()` רץ בסוף app.js, שנטען (script defer) *לפני* fretboard-scale.js, כך ש-`FretboardScale` עדיין לא קיים באותה נקודה; קריאה ישירה ל-`FretboardScale.mountPosControls()` שם זרקה חריגה ש"קטעה" את שאר `initDromoi()` בשקט (כולל בניית סרגל העריכה ו-`renderDromos()` הראשוני). הוחזר למימוש הידני המקורי עם הערה מסבירה בקוד. תיקון אמיתי (סידור-מחדש של תגי ה-script) נשקל ונדחה — סיכון רחב-טווח על כל סדר האתחול של האפליקציה, לא שווה תמורת ניקוי-קוד שולי.
- **`js/theory-lab.js`'s `posWrap`/`modeWrap` — תוקן הפער האמיתי**: לא הייתה לוגיקת auto-reposition בכלל בשינוי מספר-מיתרים (בניגוד לשאר 3 המימושים) — עכשיו קורא ל-`findBestPositionForStringMode` כמו כולם. אומת: מעבר בין 1/3/4 מיתרים על חיג'אז זז לפוזיציה הנכונה (0/7/0 בהתאמה, תואם חישוב ישיר).
- **`js/dromos-road.js`'s `rootRow`/`modeRow`/`posRow` — נבדק, לא נמצא באג**: `availablePositionBases()` הקיים כבר שומר `posBase` תקין ומציג רק פוזיציות שבאמת מנצלות את מספר המיתרים שנבחר — עדיף בפועל מ"ניחוש אחד הכי טוב". לא מוזג ל-`mountPosControls` (מרקאפ שונה במכוון: root-select אמיתי, סינון עצמאי) — נשאר כמו שהוא.
- **road-strip אוחד**: `DromosRoad.buildStripFromRoad` יוצא עכשיו עם `opts.onStopClick` אופציונלי; `theory-lab.js`'s `buildRoadStrip` העצמאי הוחלף בקריאה אליו (עם `onStopClick` שמדליק את הבהוב-הנקודה-בגריף הייחודי למסך). **תוצר לוואי**: theory-lab.js מציג עכשיו מספרי-אצבע ברצועת הכביש — לא הציג בכלל קודם.

**מסקנה כללית מהסבב הזה**: לא כל "כפילות שנראית זהה" באמת ניתנת לאיחוד בבטחה — app.js's pos-bar נראה זהה ל-mountDromosScalePanel אבל יש אילוץ סדר-טעינה שהופך איחוד מכני לסיכון אמיתי (שנתפס רק בבדיקה בדפדפן, לא בקריאת קוד). ההחלטה לא לאחד שם, ולהשאיר dromos-road.js כמו שהוא, היא סגירה מודעת של הסעיף — לא דחייה.

---

## מפת קבצים מהירה לפיצ'רים האחרונים

| קובץ | תפקיד |
|---|---|
| `js/fretboard-mirror.js` | מודול מירור (state + UI + תיקון גלילה) |
| `js/playback-speed.js` | מודול בקרת מהירות |
| `js/dromos-overrides.js` | מילון overrides לפוזיציה/אצבוע |
| `js/fretboard-scale.js` | המנוע המרכזי — `buildScaleDegreePath`, `mountDromosScalePanel`, `editDotTap`, `renderEditableDromosScale`, `onDotClick` |
| `js/worksheets.js` | דפי עבודה להדפסה — פרטבורד מסלול, לחיצת תו, לכידה להדפסה/שמירה |
| `js/app.js` | `drawFretboard` (מנוע ראשי), מסך `dromoi` עם עורך פוזיציות עצמאי |
| `tools/smoke-test.js` | בדיקת עשן (script tags, syntax, ניווט מסכים) — רץ ב-CI |

לפרטי ארכיטקטורה כלליים ראו `APP-OVERVIEW.md`.
